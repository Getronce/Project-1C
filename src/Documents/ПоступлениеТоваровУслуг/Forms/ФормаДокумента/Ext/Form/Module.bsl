
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка) 
	Если Параметры.Ключ.Пустая() Тогда
        
        ЗначениеКонстанты = Константы.ОсновнойСклад.Получить();
        
        Если ЗначениеЗаполнено(ЗначениеКонстанты) Тогда
            Объект.Склад = ЗначениеКонстанты;
        Иначе
            Сообщить("Не указан Основной склад!");
        КонецЕсли;
        
    КонецЕсли;
КонецПроцедуры
